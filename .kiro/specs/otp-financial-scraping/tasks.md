# Implementation Plan

## Tasks

- [ ] 1. 基盤整備とデータベーススキーマ拡張
- [ ] 1.1 VAPID鍵の生成と環境変数設定
  - web-pushツールを使用してVAPID公開鍵と秘密鍵のペアを生成する
  - 環境変数ファイルにVAPID_PUBLIC_KEY、VAPID_PRIVATE_KEY、VAPID_SUBJECTを追加する
  - バックエンドの環境変数読み込み設定を更新する
  - _Requirements: 9.2_

- [ ] 1.2 Prismaスキーマの拡張とマイグレーション
  - Jobモデルにstatus='waiting_for_otp'、otpAuthMethod、otpRequestedAtフィールドを追加する
  - AccountモデルにencryptedTotpSecret、totpSecretIv、totpSecretAuthTagフィールドを追加する
  - PushSubscriptionモデルを新規作成し、endpoint、p256dhKey、authKeyフィールドを定義する
  - Prismaマイグレーションを実行してスキーマ変更を適用する
  - _Requirements: 1.2, 3.2, 6.3, 10.1_

- [ ] 1.3 (P) 新規依存ライブラリのインストール
  - バックエンドにweb-pushライブラリをインストールする
  - バックエンドにotplibライブラリをインストールする
  - TypeScript型定義が正しく認識されることを確認する
  - _Requirements: 2.1, 6.5_

- [ ] 2. バックエンドサービス実装（Push通知）
- [ ] 2.1 Push Subscriptionサービスの実装
  - サブスクリプション情報の保存処理を実装する（エンドポイントURLによる重複チェック含む）
  - サブスクリプションの削除処理を実装する
  - サブスクリプションの取得と存在確認処理を実装する
  - Zodスキーマでリクエストバリデーションを定義する
  - _Requirements: 3.2, 3.3, 3.4_

- [ ] 2.2 Push Subscriptionルートの実装
  - POST /api/push/subscribe エンドポイントを実装する
  - DELETE /api/push/unsubscribe エンドポイントを実装する
  - GET /api/push/status エンドポイントを実装する
  - GET /api/push/vapid-public-key エンドポイントを実装する
  - _Requirements: 3.2, 3.3, 9.2_

- [ ] 2.3 Push Notification サービスの実装
  - VAPID認証を使用したWeb Push送信機能を実装する
  - OTP要求通知のペイロード構築処理を実装する（タイトル、本文、アクションボタン）
  - タイムアウト通知の送信処理を実装する
  - 送信失敗時のリトライ処理（1回）を実装する
  - OTPをログに記録しないようにする
  - _Requirements: 2.1, 2.2, 2.3, 2.5, 2.6, 7.3, 9.3_

- [ ] 3. バックエンドサービス実装（TOTP）
- [ ] 3.1 (P) TOTPサービスの実装
  - otplibを使用したRFC 6238準拠のOTP生成処理を実装する
  - TOTPシークレットの暗号化保存処理を実装する（既存のAES-256-GCM暗号化を使用）
  - TOTPシークレットの復号と削除処理を実装する
  - Base32形式のシークレットバリデーションを実装する
  - _Requirements: 6.1, 6.3, 6.5, 9.4_

- [ ] 3.2 TOTPシークレット管理ルートの実装
  - POST /api/accounts/:id/totp-secret エンドポイントを実装する
  - DELETE /api/accounts/:id/totp-secret エンドポイントを実装する
  - GET /api/accounts/:id/totp-status エンドポイントを実装する
  - _Requirements: 6.3, 6.4_

- [ ] 4. バックエンドサービス実装（ジョブとスクレイパー）
- [ ] 4.1 JobServiceの拡張
  - waiting_for_otpステータスの管理処理を追加する
  - OTP認証方式と要求時刻の記録処理を追加する
  - OTP待機中ジョブの一覧取得処理を追加する
  - 未完了OTP待機ジョブのリカバリ処理を追加する（起動時に5分以上経過したジョブを失敗処理）
  - _Requirements: 1.2, 10.1, 10.2, 10.3_

- [ ] 4.2 BaseScraperへのOTP検知機能追加
  - OTP画面検知メソッドを実装する（認証方式の判定含む）
  - OTP入力待機メソッドを実装する（5分タイムアウト）
  - OTP入力・送信メソッドを実装する
  - OTP待機中のブラウザセッション維持処理を実装する
  - _Requirements: 1.1, 1.3, 1.4, 5.3, 7.1, 7.4_

- [ ] 4.3 ScraperServiceのOTP対応
  - OTP検知時のジョブステータス更新とPush通知送信を統合する
  - TOTP事前登録済みの場合の自動OTP生成・入力処理を実装する
  - OTP受信コールバック機構（EventEmitter）を実装する
  - OTP入力後のスクレイピング再開処理を実装する
  - _Requirements: 1.1, 1.2, 5.2, 5.4, 5.5, 5.6, 5.7, 6.1, 6.2_

- [ ] 4.4 OTP送信APIの実装
  - POST /api/scraping/:jobId/submit-otp エンドポイントを実装する
  - POST /api/scraping/:jobId/cancel-otp エンドポイントを実装する
  - GET /api/scraping/:jobId/otp-status エンドポイントを実装する
  - OTPは6-8桁の数字のみ受け付けるバリデーションを実装する
  - OTP入力3回失敗時のスクレイピング中止処理を実装する
  - _Requirements: 5.1, 8.2, 8.4_

- [ ] 5. フロントエンド基盤（Service Worker）
- [ ] 5.1 vite-plugin-pwa設定の変更
  - vite.config.tsをinjectManifest戦略に変更する
  - カスタムService Workerのソースファイル設定を追加する
  - 開発モードでのService Worker有効化設定を追加する
  - 既存のWorkboxキャッシング設定を移行する
  - _Requirements: 11.4_

- [ ] 5.2 カスタムService Workerの実装
  - pushイベントハンドラーを実装して通知を表示する
  - notificationclickイベントハンドラーを実装してアプリをフォーカスまたはオープンする
  - クライアントへOTP入力ダイアログ表示メッセージを送信する処理を実装する
  - プリキャッシュマニフェストの注入処理を実装する
  - _Requirements: 11.1, 11.2, 11.3, 11.5, 2.4_

- [ ] 6. フロントエンドUI実装
- [ ] 6.1 Push通知サブスクリプション管理フックの実装
  - ブラウザの通知許可状態を確認する処理を実装する
  - Push通知の許可リクエスト処理を実装する
  - サブスクリプションの登録・解除処理を実装する
  - VAPID公開鍵の取得とPushManager.subscribe呼び出しを実装する
  - _Requirements: 3.1, 3.5_

- [ ] 6.2 (P) 設定画面へのPush通知設定UIの追加
  - Push通知の有効/無効切り替えUIを追加する
  - 現在の許可状態を表示するUIを追加する
  - サブスクリプションが無効になった場合の再登録促進UIを追加する
  - _Requirements: 3.1, 3.5_

- [ ] 6.3 OTP入力ダイアログコンポーネントの実装
  - モーダルダイアログのUI構造を実装する
  - 対象アカウント名（金融機関名）の読み取り専用表示を実装する
  - 数字のみ6-8桁を受け付けるOTPコード入力フィールドを実装する
  - 送信ボタンとキャンセルボタンを実装する
  - 残り時間のカウントダウン表示を実装する
  - _Requirements: 4.1, 4.2, 4.3, 4.4, 4.5, 4.6_

- [ ] 6.4 OTP入力ダイアログの状態管理とAPI連携
  - Service Workerからのメッセージ受信によるダイアログオープン処理を実装する
  - OTP送信のmutation処理を実装する
  - キャンセル時のジョブ中止API呼び出しを実装する
  - エラーメッセージ表示処理を実装する（誤OTP、最大リトライ超過）
  - _Requirements: 4.1, 4.5, 5.1, 8.1, 8.3_

- [ ] 6.5 (P) TOTPシークレット設定UIの実装
  - アカウント編集画面にTOTPシークレット入力フィールドを追加する
  - TOTPシークレットの登録・削除UIを実装する
  - TOTPシークレット登録状態の表示を実装する
  - _Requirements: 6.4_

- [ ] 6.6 (P) ダッシュボードへのOTPエラー表示追加
  - OTP関連エラーが発生したジョブの情報表示を追加する
  - エラー種別（タイムアウト、最大リトライ超過等）の表示を実装する
  - _Requirements: 8.5_

- [ ] 7. 金融機関スクレイパーへのOTPセレクター追加
- [ ] 7.1 (P) 既存スクレイパーへのOTPセレクター定義追加
  - 各金融機関スクレイパー（三菱UFJ、三井住友、SBI新生、楽天銀行、楽天証券、ポケットカード）にOTP画面のセレクターを定義する
  - 認証方式の判定ロジックを各スクレイパーに追加する
  - OTP入力フィールドと送信ボタンのセレクターを定義する
  - _Requirements: 1.1, 1.3_

- [ ] 8. テストとセキュリティ検証
- [ ] 8.1 バックエンドユニットテストの実装
  - TotpService.generateOtpのRFC 6238準拠テストを実装する
  - PushNotificationService.sendOtpRequiredNotificationのペイロード構築テストを実装する
  - JobService.setWaitingForOtpのステータス遷移テストを実装する
  - PushSubscriptionServiceのCRUD操作テストを実装する
  - _Requirements: 6.5, 2.2, 1.2, 3.2_

- [ ] 8.2 (P) セキュリティ要件の検証テスト
  - OTPがログに記録されないことを確認するテストを実装する
  - TOTPシークレットが暗号化されて保存されることを確認するテストを実装する
  - OTPがメモリ上でのみ処理されることを確認するテストを実装する
  - HTTPS環境チェックのテストを実装する
  - _Requirements: 9.1, 9.3, 9.4, 9.5_

- [ ] 8.3 統合テストの実装
  - Push Subscription登録から通知送信までのフローテストを実装する
  - OTP送信からスクレイパー再開までの連携テストを実装する
  - TOTP自動生成からOTP自動入力までの連携テストを実装する
  - タイムアウト処理のテストを実装する
  - _Requirements: 2.1, 5.2, 6.2, 7.2_

- [ ] 9. アプリケーション起動時のリカバリ処理統合
- [ ] 9.1 起動時リカバリ処理の統合
  - アプリケーション起動時に未完了OTP待機ジョブを検出する処理を統合する
  - 5分以上経過したOTP待機ジョブを失敗処理する
  - 必要に応じて再度OTP通知を送信する処理を実装する
  - _Requirements: 10.2, 10.3_

## Requirements Coverage

| Requirement | Tasks |
|-------------|-------|
| 1.1, 1.2, 1.3, 1.4 | 1.2, 4.1, 4.2, 4.3, 7.1 |
| 2.1, 2.2, 2.3, 2.4, 2.5, 2.6 | 1.3, 2.3, 5.2, 8.1 |
| 3.1, 3.2, 3.3, 3.4, 3.5 | 2.1, 2.2, 6.1, 6.2 |
| 4.1, 4.2, 4.3, 4.4, 4.5, 4.6 | 6.3, 6.4 |
| 5.1, 5.2, 5.3, 5.4, 5.5, 5.6, 5.7 | 4.3, 4.4, 6.4, 8.3 |
| 6.1, 6.2, 6.3, 6.4, 6.5 | 3.1, 3.2, 4.3, 6.5, 8.3 |
| 7.1, 7.2, 7.3, 7.4 | 2.3, 4.2, 8.3 |
| 8.1, 8.2, 8.3, 8.4, 8.5 | 4.4, 6.4, 6.6 |
| 9.1, 9.2, 9.3, 9.4, 9.5 | 1.1, 2.2, 2.3, 3.1, 8.2 |
| 10.1, 10.2, 10.3, 10.4 | 1.2, 4.1, 9.1 |
| 11.1, 11.2, 11.3, 11.4, 11.5 | 5.1, 5.2 |
